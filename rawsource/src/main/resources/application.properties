spring.application.name=rawSource

# Server config
server.port=8443

# SSL configuration
server.ssl.enabled=true
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=mipassword
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=miCertificado

# Datasource config
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=q1w2e3r4t5.larm
spring.datasource.url=jdbc:postgresql://34.59.103.55:5432/rawsource
spring.jpa.hibernate.ddl-auto=update

# Rate limiting configuration
rate.limit.login.requests=5
rate.limit.login.window.minutes=5
rate.limit.general.requests=100
rate.limit.general.window.hours=1
rate.limit.critical.requests=50
rate.limit.critical.window.hours=1

# Blacklist configuration
rate.limit.blacklist.threshold=10
rate.limit.blacklist.duration.hours=1

# Cleanup configuration
rate.limit.cleanup.interval.hours=1

# Logging configuration
logging.level.root=INFO
logging.level.com.example.rawsource=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.validator=WARN
logging.level.org.hibernate.validator.internal.util.Version=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN

# Log pattern
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Log file
logging.file.name=logs/rawsource.log
logging.file.max-size=10MB
logging.file.max-history=30

# JWT Configuration
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000
